function nums(x: number): number {
  return x;
}

console.log(nums(12));

function printHello(name: string): string {
  return "Hello my name : " + name;
}

console.log(printHello("Mikhael"));

function sayHello(name: string): void {
  console.log("My name : " + name);
}

sayHello("mikhael");
